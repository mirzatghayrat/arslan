# Optional bearer token protecting the API + WebSocket. Leave blank to disable auth (local only).
ARSLAN_API_TOKEN=

# Secret used to derive the encryption key for stored LLM API keys. Set a long random value in production.
ARSLAN_SECRET_KEY=

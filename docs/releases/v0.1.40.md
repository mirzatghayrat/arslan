# Arslan 0.1.40

## Upgrade safety — read before installing

HOLD: not ready to publish. Candidate CI, user merge and final signed-package
verification are pending; do not remove this hold until those checks finish.

This version upgrades the database when migrations are pending. Before those
migrations it automatically creates a checksummed backup in the data directory's
`backups/` folder and retains the latest three automatic upgrade backups.
On macOS the default is `~/Library/Application Support/Arslan/backups/`.
If backup fails, migration must not proceed. Backups do **not** include the
external encryption key. Keep that key separately and securely.

To return to 0.1.38, stop Arslan and restore a **pre-0.1.40 upgrade backup** using
the **same encryption key**, then open the restored profile with 0.1.38. Merely
reinstalling the old app over an upgraded database is not a supported rollback.
A backup made after upgrading is not a substitute for the pre-upgrade backup.

Settings → Memory & Data → Create backup creates a separate manual backup,
shows its location, then restarts Arslan. Finish current tasks and save drafts
first. Manual backups are not removed by the three-upgrade-backup retention rule.
Backups contain private data: keep them and the separate key secure.

## Known limitations

跨语言或多文档对比时，模型可能错误地断言某份文档缺少某项内容；请打开来源核对。来源链接用于导航，不是事实核查。

R4 failed and was explicitly accepted as a known limitation by the product owner
on 2026-09-24; it has not been changed to a pass. Eleven other cases have core
passing evidence with documented caveats, not a universal factual guarantee.
The experimental mandatory source-review gate is disabled by default.

## What changed

- Clearer desktop version/channel display and a less crowded conversation view.
- Document/source-reading fixes and clearer read, excerpt and truncation labels.
- Project-memory and task-continuation reliability improvements within the
  daily research-assistant scope; this is not completion of the full v1.2 plan.
- Pre-migration backups and the native manual-backup entry described above.

## Release evidence

Isolated development-package upgrade, native backup and restore checks are
recorded in `docs/companion/stable-0.1.40-native-backup-20260924.md`. Synthetic
profiles only; not a claim that arbitrary existing user data was tested.

HOLD: final merged source SHA, same-source CI, signing, notarization, stapling,
Gatekeeper, fresh-install, DMG layout/Finder and update-asset verification pending.
Draft only. Merge and Publish are user-controlled.

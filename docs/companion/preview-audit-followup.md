# Preview release audit follow-up — 2026-09-19

Accepted: beta.2 is not publishable on the strength of the Release workflow
alone. Its exact tagged source did not finish CI. Existing tags remain immutable;
beta.3 must first pass exact-source CI and then a new signed artifact build.

Latest e42952e7 CI failure was verified: generated capability inventory stale
after version changes, plus four needless generic-argument borrows in Rust.
Those are corrected without weakening their gates. Local Rust tests: 77 passed,
one existing opt-in fixture ignored; fmt and all-target Clippy pass. Inventory,
manifest and release-policy tests: 22 passed. The release workflow now refuses
to build without a successful CI run for the same SHA, fails closed on GitHub
query errors, and loads the versioned release notes rather than relying only
on autogenerated changelogs.

The user's screenshot correctly warns about shared stable/beta identity and
in-place data migration. This warning is now first in beta.3 notes and the held
beta.2 draft. No downgrade compatibility or automatic rollback is asserted.
The isolated analysis Python runtime is intentionally retained for this preview;
removing it would change advertised functionality and invalidate its isolation
evidence. Size optimization is not folded into this release repair.

Two qualifications to the screenshot: the Release workflow does run 65
fresh-install checks (not the full CI), and the beta.2 draft already had the
manual notes at the time of this review, though the backup warning was too low.

## Retired protocol test check

At 1e095252, `tests/server/test_tool_loop.py` changed its scripted adapter from
the retired text protocol to native calls. Parent and current file each contain
38 test functions, not a blanket deletion of 34 tests. Five names changed:
multiple native calls; preserving non-protocol JSON; bounded unverified-claim
repair; honest answers without tools; bounded forward-promise repair. The
tool-loop/native-loop/runtime-policy/task-entrypoint/confirmation selection
passes 79 cases with no skips. This is scoped runtime evidence, not proof that
all former assertions or real-model behavior are equivalent.

W19 remains limited to public read-only browsing, an ephemeral isolated profile,
HTTPS/public-network policy and no credentials/form/upload/non-GET authority.
Its documented lack of independently reviewed OS broker isolation remains a
release-note limitation; authenticated/general interaction is not advertised.
No threat-model completion, genuine account test or full v1.2 acceptance is
claimed by this limited preview repair.
